# Runtime dependencies for the deployed API only (main.py, agent.py,
# db_tools.py). The data-science libs in requirements.txt are only
# needed for the one-time local data-prep scripts (explore_data.py,
# detect_anomalies.py, push_to_supabase.py) and aren't installed here
# to keep the deploy build fast and light.
fastapi==0.138.1
uvicorn==0.49.0
supabase==2.31.0
groq==1.5.0
python-dotenv==1.2.2
